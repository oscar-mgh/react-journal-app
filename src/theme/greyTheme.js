import { createTheme } from '@mui/material';

export const greyTheme = createTheme({
	palette: {
		primary: {
			main: '#29323b',
		},
		secondary: {
			main: '#1a5fdf',
		},
		error: {
			main: '#da0909',
		},
	},
});
